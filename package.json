{
  "name": "score-keeper",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev --port 6161",
    "dev:static": "node server.mjs",
    "deploy": "wrangler deploy",
    "db:migrate": "wrangler d1 migrations apply score-keeper --remote",
    "db:migrate:local": "wrangler d1 migrations apply score-keeper --local"
  },
  "devDependencies": {
    "wrangler": "^4.131.0"
  }
}
